# Issue 62: Domoticz-Home-Connect-Plugin not detected

Status: closed; shipped in `v2.13.0`.

## Intent
The user `Eddie-BS` wants to add the plugin `mario-peters/Domoticz-Home-Connect-Plugin` to the PyPluginStore registry.

## Analysis
- Repo: `mario-peters/Domoticz-Home-Connect-Plugin`
- Action: Add to `registry.json` using standard format.
- Check default branch and platform support. Usually `master` or `main`. I will check GitHub API to confirm.

## Outcome
- Added `mario-peters/Domoticz-Home-Connect-Plugin` to `registry.json`.
- Closed on 2026-06-29.
